# 股市风云（Stock Market）— 工程审计报告

| 字段 | 内容 |
|------|------|
| 报告日期 | 2026-08-11 |
| 工程目录 | `stockmarket-1.21.1` |
| 技术栈 | NeoForge 1.21.1 / Java 21 / ApricityUI 1.2.1（**硬依赖**） |
| 构建状态 | ✅ `./gradlew build` BUILD SUCCESSFUL |
| 单元测试 | ✅ 15/15 全绿 |

---

## 1. 工程建立（Phase 0）

- 基于 `examplemod-template-1.21.1.zip` 模板解压清理：`mod_id=stockmarket`、`mod_name=Stock Market`、包 `com.tanrunn.stockmarket`
- **AUI 硬依赖**：`implementation "com.sighs:ApricityUI-neoforge-1.21.1:1.2.1"`；`mods.toml` 声明 `apricityui` `type="required"` `side="CLIENT"`
- 删除模板示例内容（方块/物品/标签）、`.idea`、`TEMPLATE_LICENSE.txt`

## 2. 功能清单

### 服务端（权威）
| 模块 | 说明 |
|------|------|
| `StockRegistry` | 数据包驱动股票定义（`data/stockmarket/stocks/*.json`），`/market reload` 热重载 |
| `PriceModel` | 几何随机游走（纯函数，可复现） |
| `MarketService` | 每 `tickInterval`（默认 100 tick≈5s）全市场更新 + 日切 + 委托撮合 + 查看者自动推送 |
| `TradeEngine` | 市价买卖 / 限价单预留·成交·退款（纯函数，全部可单测） |
| `OrderBook` | 限价单簿：买单现价≤限价成交、卖单现价≥限价成交，整单按委托价成交；随世界持久化 |
| `AccountData` | 玩家账户附件（初始资金 1000、持仓），随玩家存档 |
| `MarketSavedData` | 股票价格 + 日 K 历史（40 根）+ 委托簿，跨重启持久化 |
| `MarketCommand` | `/market`、`list`、`account`、`buy/sell`、`order buy/sell/cancel/list`；OP：`reload/reset/setprice` |

### 行情与 K 线
- 每只股票维护日 K（开/收/高/低/量），游戏日切换生成蜡烛，保留近 40 根
- 新世界自动预生成 30 根历史 K 线，开服即有图
- **AUI Canvas 绘制**（Java 侧 `Canvas.getContext("2d")`）：网格线 + 蜡烛（红涨绿跌）+ 影线 + 收盘价金线
- 行情列表红涨绿跌（中式习惯）

### ApricityUI 界面（`screens/market.html`）
- 行情列表（点击选股）、账户栏、我的委托栏、数量步进器、市价买卖、刷新
- 打开面板 → 服务端登记为查看者，**每 5 秒自动推送快照**（价格/K线实时跳动）；Esc 关闭 → 通知服务端停止推送
- 所有交互 Java 绑定（沿用黄历 mod 验证过的模式，不依赖页面脚本）

### 网络包
- `MarketRequestC2S`（open/close/refresh）、`TradeRequestC2S`、`MarketSnapshotC2S`（股票+账户+委托+K线历史）

## 3. 验证

- ✅ `./gradlew build`：15/15 单元测试全绿（PriceModel 4 / Stock 2 / TradeEngine 9）
- ⚠️ 服务端启动冒烟**待上游修复**：AUI 是客户端 mod，dev `runServer` 会把它放进运行时 classpath 导致专用服加载崩溃——已加 `-PserverOnly` 开关降级 `compileOnly` 规避（生产服务器只装本 mod 无影响）；等 AUI 作者修复后移除
- ⚠️ 客户端 UI 实机效果（面板/K线/推送）待 runClient + MCP 验证

## 4. 已知限制 / 待办

- 限价单为**整单成交**（无部分成交）；成交价取委托价
- 无涨跌停/熔断、开收盘时间（后续配置项）
- 无公司事件/新闻、无大盘指数
- 无跨服同步（单服存档）
