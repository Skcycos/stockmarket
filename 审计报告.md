# 股市风云（Stock Market）— 工程审计报告

| 字段 | 内容 |
|------|------|
| 报告日期 | 2026-08-12 |
| 工程目录 | `stockmarket` |
| 技术栈 | NeoForge 1.21.1 / Java 21 / ApricityUI 1.2.1（硬依赖） |
| 单元测试 | ✅ 54/54 全绿 |
| 本次复核 | 拆股冻结资金、AUI 刷新绑定、幂等账本、协议与响应式布局已修复 |

## 1. 工程与功能现状

- 服务端权威处理行情、账户、手续费、数量/价格门禁、限价委托、撮合、撤单和持久化。
- `OrderBook.Order` 记录买单创建时的精确冻结现金；拆股只调整价格和数量，不重算冻结金额。
- 拆股后的成交与撤单都使用创建时的冻结金额，避免浮点舍入或拆股导致资金漂移。
- `AccountData` 保留完整余额流水，`requestId` 幂等记录不再因 200 条上限被截断。
- 市场快照把服务端 `maxOrderQty` 传给客户端，AUI 数量步进器不再写死 9999。
- ApricityUI `Document.refresh()` 产生新刷新代际时，Java 侧会重新安装所有 DOM 监听器；页面更新仍在客户端线程执行。
- 已加入客户端专用的 `LocalStorage` 兼容补丁，使用 NeoForge 1.21.1 的 `NbtIo(Path, NbtAccounter)` API，避开 AUI 1.2.1 的旧 `File` 反射签名。
- 网络 payload 协议版本已从 `1` 提升到 `2`，与快照编解码字段变更保持一致。

## 2. ApricityUI 页面

- 页面资源为 `assets/apricityui/apricity/screens/market.html`。
- 行情、持仓、委托、筛选/排序、批量卖出/撤单、限价买卖、K 线和消息均由 Java 绑定。
- AUI 刷新会重建子元素，因此绑定逻辑按 `Document.getRefreshGeneration()` 去重并重新绑定，避免推送几次后按钮失效。
- 面板高度使用 `88vh`，移除 540px 的最小高度，480px 高度窗口不再被强制裁剪。

## 3. 验证记录

- ✅ `./gradlew test --rerun-tasks`：54/54 全绿。
- ✅ `git diff --check`：无空白错误。
- ✅ `./gradlew build --rerun-tasks`：`BUILD SUCCESSFUL`。
- ✅ `./gradlew runServer -PserverOnly`：专用服生成世界并完成启动，日志出现 `Stock market initialized`；进程随后手动停止。
- ✅ `./gradlew runClient`：客户端完成 AUI 资源扫描和 `CLIENT SETUP`，`LocalStorageMixin` 与 `StorageAccessor` 成功应用；日志不再出现旧 `NbtIo` 反射异常；进程随后手动停止。
- ℹ️ 本次启动验证覆盖加载/注册/资源/兼容层，未伪造联网玩家点击；真实 `/market` 交易仍应在目标整合服做一次在线验收。

## 4. 保留的产品限制

- 限价单为整单成交，无部分成交。
- 暂无涨跌停/熔断和开收盘时段配置。
- 暂无跨服同步，数据以单服世界存档为准。
